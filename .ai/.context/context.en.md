# ENTERPRISE SYSTEM CONTEXT & STRICT GENERATION BLUEPRINT
## SYSTEM IDENTITY: MULTI-TENANT MEMBERSHIP HUB & ATTENDANCE CORE ENGINE
## PACKAGING ROOT: org.nlh4j.saas.membership.hub.*
## SOURCE CODE TARGET: ./sources/

---

## 1. SYSTEM OVERVIEW & ARCHITECTURE

### 1.1. Mission Statement
The system is designed as an Enterprise SaaS (Software-as-a-Service) Multi-Tenant solution for managing students, membership cards, and intelligent check-ins via dynamic QR codes. The platform concurrently serves two main user spaces:
1. Web Admin (B2B): Tailored for center/gym/academy owners to manage business operations, configure membership packages, and monitor traffic metrics.
2. Mobile App (B2C): Engineered for students to perform dynamic QR check-ins, view real-time card validities, and receive instant cross-channel push updates.

### 1.2. Core Architecture Data Flow
The system strictly enforces an Event-Driven Architecture (EDA) paired with a CQRS / Reactive Core to sustain massive concurrent throughput during peak checking hours (class shift changes, high-traffic gym hours):
```
[Mobile App QR Scan] -> [Quarkus Gateway/API (Tenant Filter)]
                            | (Idempotent Check & Update DB)
                            |--> [PostgreSQL (Discriminator: tenant_id)]
                            |
                            |--> [Kafka: attendance-events] (Reactive Emit)
                                        |
                                        |--> [Kafka Consumer / Dispatcher Router]
                                                 |--> [Topic: zalo-personal] -> API Zalo OA (User)
                                                 |--> [Topic: zalo-group]    -> API Zalo Group CRM
                                                 |--> [Topic: mobile-push]   -> Firebase FCM Engine
```

---

## 2. TECH STACK SPECIFICATIONS

All source code generated by the AI must precisely use the versions, dependencies, and ecosystem libraries defined below:

### 2.1. Backend Core Stack
- Framework: Quarkus 3.x (Java 21 LTS), fully utilizing reactive paradigms powered by Mutiny Framework and Hibernate Reactive with Panache.
- Dependency Injection: CDI (Contexts and Dependency Injection) leveraging standard Quarkus ArC compile-time processing.
- REST Engine: RESTEasy Reactive backed by Jackson as the universal JSON binding provider.
- Database Driver: Vert.x Reactive PostgreSQL Client.
- Messaging Service: Quarkus SmallRye Reactive Messaging integrated with the native Apache Kafka Client layer.
- Security: Quarkus OIDC (OpenID Connect) Reactive, bound with JOSE / Nimbus-JWT libraries for cryptographic token verification.
- Backend Extensions: quarkus-hibernate-reactive-panache, quarkus-reactive-pg-client, quarkus-smallrye-reactive-messaging-kafka, quarkus-resteasy-reactive-jackson, quarkus-oidc, mapstruct, and **quarkus-smallrye-openapi**.

### 2.2. Frontend & Hybrid Mobile Stack
- Web/Mobile Native Base: Next.js 14+ (App Router) executing strictly typed TypeScript (strict: true).
- Internationalization (i18n): next-intl driving dynamic locale-aware routing patterns.
- Styles & UI: Tailwind CSS layered over Shadcn UI / Radix Primitives.
- Mobile Runtime Wrapper: Capacitor Native Layer to package web bundles directly into Apple App Store and Google Play Store binaries.

---

## 3. MULTI-TENANCY POSTGRESQL ARCHITECTURE

The system implements a Shared Database, Discriminator Column tenancy paradigm. Every domain table interacting with tenant operations must enforce data isolation using a mandatory tenant_id column.

### 3.1. Database Schema DDL (Flyway / Liquibase Spec)
Target Path: ./sources/backend/src/main/resources/db/migration/V1.0.0__init_schema.sql
```
CREATE TABLE sys_tenants (
    id VARCHAR(50) PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    company_code VARCHAR(100) UNIQUE NOT NULL,
    status VARCHAR(20) NOT NULL DEFAULT 'ACTIVE',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE app_users (
    id VARCHAR(50) PRIMARY KEY,
    tenant_id VARCHAR(50) NOT NULL REFERENCES sys_tenants(id),
    email VARCHAR(191) NOT NULL,
    password_hash VARCHAR(255),
    full_name VARCHAR(255),
    provider VARCHAR(20) NOT NULL DEFAULT 'INTERNAL',
    provider_id VARCHAR(255),
    status VARCHAR(20) NOT NULL DEFAULT 'ACTIVE',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_user_tenant_email UNIQUE (tenant_id, email)
);

CREATE TABLE student_cards (
    id VARCHAR(50) PRIMARY KEY,
    tenant_id VARCHAR(50) NOT NULL REFERENCES sys_tenants(id),
    student_id VARCHAR(50) NOT NULL REFERENCES app_users(id),
    card_number VARCHAR(100) NOT NULL,
    remaining_days INT NOT NULL,
    expired_at TIMESTAMP WITH TIME ZONE NOT NULL,
    status VARCHAR(20) NOT NULL DEFAULT 'ACTIVE',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_card_tenant_number UNIQUE (tenant_id, card_number)
);

CREATE TABLE attendance_logs (
    id VARCHAR(50) PRIMARY KEY,
    tenant_id VARCHAR(50) NOT NULL REFERENCES sys_tenants(id),
    student_id VARCHAR(50) NOT NULL REFERENCES app_users(id),
    card_id VARCHAR(50) NOT NULL REFERENCES student_cards(id),
    checkin_date DATE NOT NULL,
    checked_in_at TIMESTAMP WITH TIME ZONE NOT NULL,
    CONSTRAINT uq_attendance_day UNIQUE (tenant_id, student_id, checkin_date)
);

CREATE INDEX idx_users_tenant ON app_users(tenant_id);
CREATE INDEX idx_cards_tenant_student ON student_cards(tenant_id, student_id);
CREATE INDEX idx_attendance_tenant_date ON attendance_logs(tenant_id, checkin_date);

CREATE TABLE sys_promotions (
    id VARCHAR(50) PRIMARY KEY,
    tenant_id VARCHAR(50) NOT NULL REFERENCES sys_tenants(id),
    title VARCHAR(255) NOT NULL,
    description TEXT,
    discount_rate DECIMAL(5,2) DEFAULT 0.00, -- Ví dụ: 10.00%
    start_date DATE NOT NULL,
    end_date DATE NOT NULL,
    status VARCHAR(20) NOT NULL DEFAULT 'ACTIVE', -- ACTIVE, EXPIRED, DISABLED
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE notification_delivery_logs (
    id VARCHAR(50) PRIMARY KEY,
    tenant_id VARCHAR(50) NOT NULL REFERENCES sys_tenants(id),
    student_id VARCHAR(50) NOT NULL REFERENCES app_users(id),
    channel VARCHAR(20) NOT NULL, -- ZALO_PERSONAL, ZALO_GROUP, MOBILE_PUSH_FCM
    template_type VARCHAR(50) NOT NULL, -- EXPIRATION_ALERT, PROMOTION, ATTENDANCE
    payload TEXT NOT NULL, -- Nội dung JSON đã gửi đi
    delivery_status VARCHAR(20) NOT NULL DEFAULT 'PENDING', -- PENDING, SENT, FAILED
    error_message TEXT, -- Lưu vết nếu gọi API bên thứ ba bị lỗi
    sent_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_promotions_tenant ON sys_promotions(tenant_id, status);
CREATE INDEX idx_notif_logs_tenant_student ON notification_delivery_logs(tenant_id, student_id);
```

### 3.2. Automated Tenant Isolation Constraints
When generating Entity or Repository layers, the AI must explicitly supply Hibernate @FilterDef and @Filter properties. This configuration forces the runtime injection of the tenant_id = :tenantId predicate across all generated SELECT, UPDATE, and DELETE SQL statements. Manual injection of tenant predicates inside raw JQL/HQL queries is forbidden.

---

## 4. HYBRID AUTHENTICATION ARCHITECTURE

The system implements a robust Dual-Engine Federated Authentication model:

### 4.1. Internal Authentication Pipeline
- Users authenticate via standard Email + Password pairings. Password storage mandates cryptographic hashing utilizing the BCrypt algorithm with a workload parameter set to a minimum of 12.
- The backend infrastructure dynamically issues a cryptographically signed, short-lived JWT Access Token utilizing asymmetric RS256 keys.

### 4.2. Social OAuth2 Federated Identity Engine
- Incorporates flexible single sign-on (SSO) abstractions for Firebase, Google, and Facebook identities.
- Just-In-Time (JIT) Provisioning Mechanism: Upon receiving a social token validation event from the Mobile Client, the backend's Reactive OIDC filter performs remote verification against the target provider's public certs. If the token signature is valid and the extracted email address does not exist within app_users for the scoped tenant, the engine triggers automated inline record provisioning under the specific provider classification.

---

## 5. IDEMPOTENT CHECK-IN LOGIC & GYM-STYLE DAY TRACKING

### 5.1. Idempotent QR Code Scan Execution
- The QR code presented on the client terminal is an asymmetric AES-256 cryptographically encrypted JSON string wrapping student_id, card_id, tenant_id, and a high-precision timestamp (expiring within 30 seconds to block malicious replay vectors like screenshot sharing).
- When the ingestion endpoint captures the scan payload, it unmarshals the content and performs an assertion check within attendance_logs bounded by the compound composite key index (tenant_id, student_id, CURRENT_DATE).
- Processing Matrix: If a record matches the current date parameters, the API must short-circuit the execution path and safely return an HTTP 200 OK status coupled with a structural duplication warning flag to protect backend write threads. If empty, the transaction persists a new validation record.

### 5.2. Dynamic Gym-Style Subscription Tracking
- Unlike standard itemized deduction schemes (debit-per-visit structures), the platform computes remaining duration dynamically.
- The remaining_days metric is never evaluated as a stagnant attribute within the data block. Instead, it is computed on-the-fly at runtime using the following formula: remaining_days = max(0, expired_at - CURRENT_DATE).
- Upon passing the initial validation threshold for the current day, the system updates active synchronization markers and safely hands over message dispatches to the asynchronous pipeline.

## 5.3. ADMISTRATIVE EXPIRATION LOGIC & PROACTIVE CUSTOMER RETENTION
- **3-Day Expiration Guard (Hàng rào cảnh báo 3 ngày):** The Web Admin console must perform reactive, runtime scanning operations on `student_cards` records where `remaining_days <= 3`. Users within this domain are programmatically flagged as `CRITICAL_EXPIRATION` to immediately render high-visibility alerts on the administrative dashboard viewports.
- **On-Demand Zalo OA Push Triggering:** Admin users can manually trigger lifecycle messaging directly from the web client dashboard layout. Upon action click, the backend Resource controller must immediately pass a non-blocking payload straight into the Kafka topic `notification-zalo-personal` passing down pre-compiled transactional metadata: *"Your pass will expire on [Date]. Please finalize renewal balances prior to [Date] to ensure uninterrupted access."*
- **Segmented Marketing Broadcasts:** Supports bulk, segmented event broadcasting via Zalo Group CRM or Zalo OA configurations to communicate new localized academic class timetables based on active student tracking indices.
- **Promotion Campaigns Orchestration:** Allows administrative operators to provision localized membership discount tokens or renewal reward parameters inside the sys_promotions data pool. Active promotion states can trigger mass messaging dispatches (Bulk Push workflows) targeting specific segmented user demographics through decoupled Kafka events.
- **Unified Notification Delivery Audit Trail:** Every transaction generating an outbound dispatch (automated check-in notices, background expiration alerts, or manual admin campaign triggers) MUST immediately commit an audit log record initialized as PENDING inside notification_delivery_logs. Once downstream message consumers capture processing responses from foreign network gateways (Zalo Open API / Firebase Infrastructure), the lifecycle status must be mutated immediately to SENT or FAILED accompanied by high-precision error logs for real-time monitoring inside the administrative console.

---

## 6. EVENT-DRIVEN NOTIFICATION & CROSS-CHANNEL KAFKA CORE

Check-in events that successfully pass the validation gate immediately transition execution control over to an asynchronous Apache Kafka cluster to isolate the user-facing web worker threads, keeping API overhead below 50ms.

### 6.1. Kafka Topic Topologies
- Root Processing Ingestion: attendance-events (Captures raw scan state, partitioned explicitly by tenant_id).
- Downstream Fan-Out Destination Splittings:
  - notification-zalo-personal: Routes transactional logs directly to individual user profiles via Zalo OA.
  - notification-zalo-group: Fires localized status reporting hooks inside designated CRM classroom channels.
  - notification-push-fcm: Translates payload snapshots into real-time visual system alerts via Firebase Cloud Messaging.

### 6.2. Non-Blocking Fan-Out Dispatch Architecture
A background Kafka Consumer orchestrates execution monitoring on the root attendance-events channel. Upon event capture, it leverages reactive streaming topologies to multiplex payload broadcasts concurrently across the three sub-channel emitters. Operations executing towards external APIs must run entirely in parallel using non-blocking asynchronous I/O threads.

### 6.3. Fault Tolerance & Poison Pill Isolation (DLQ Patterns)
All downstream consumer configurations must incorporate a structured Dead Letter Queue (DLQ) paired with an exponential backoff retry profile (capped at 3 verification attempts). If third-party integrations (e.g., Zalo API outages) trigger communication drops, fault payloads migrate to isolated DLQ domains to insulate the primary pipeline against blocking locks.

---

## 7. HYBRID MOBILE BOUNDARY & INTERNATIONALIZED SEO MECHANISMS

### 7.1. Edge-Layer Locale Recognition Middleware
The Next.js framework leverages a high-performance Edge Middleware to orchestrate user localization processing. Language resolution strictly prioritizes requests in the following sequential order:
1. Strategy 1: Intercepts active choice configurations explicitly cached within standard HTTP Client Cookies or explicit LocalStorage key registries.
2. Strategy 2: For packaged mobile shell clients running via the Capacitor runtime, queries the native device hardware subsystem directly via Device.getLanguageCode() calls.
3. Strategy 3: Analyzes standard inbound HTTP Accept-Language request header configurations.
4. System Fallback: Defaults processing localization directly to standard Vietnamese contexts (vi).

### 7.2. Global Multi-Language Metadata Execution
All public routing configurations must ingest structured SEO metadata attributes:
- Programmatically injection of alternate localized hypermedia controls, rendering standard HTML link rel="alternate" hreflang="x" href="url" header nodes to protect cross-language index parameters from duplicate content penalties on web crawlers.
- Dynamic evaluation of automated sitemap.js and robots.js configurations to separate crawler indexing indexing boundaries per localized routing target (/vi/, /en/).

## 7.3. HYBRID MOBILE NEXT.JS COMPLIANCE RAIL RULES (CAPACITOR SPEC)

To guarantee seamless execution when the Next.js App Router bundle is packaged into iOS/Android native shell viewports via Capacitor, the AI must enforce the following absolute safety rails during code generation:

### 7.3.1. Strict Client-Side Fetching & Absolute API Addressing
- **SSR Restriction:** Server-side rendering features (`getServerSideProps`, Server Components dynamic fetches) are strictly banned for mobile app viewpoints. All data ingestion inside `/app/[locale]/app/` directories must utilize pure Client-Side Fetching (`useClient`, `useEffect`, or SWR/React Query).
- **Absolute URLs:** All API endpoints targeted by the mobile shell must compile using explicit, absolute URLs (configured via runtime environment blocks like `process.env.NEXT_PUBLIC_API_BASE_URL`). Relative path routing (`/api/v1/...`) will cause absolute silent network failures inside the native file protocols (`capacitor://` or `http://localhost`).

### 7.3.2. Hydration Safeguards & Native Storage Interaction
- **Hydration Mismatch Prevention:** The AI must wrap all components reading layout states, locale cookies, or authorization markers within a `useState` + `useEffect` mounted verification gate, or leverage dynamic imports with `{ ssr: false }`. Rendering native client values directly on the initial frame execution is forbidden.
- **Abstraction Layer:** Direct calls to naked browser storage (`window.localStorage`) are restricted. The code must abstract storage interactions through a unified service tier that gracefully switches to `@capacitor/preferences` if running inside a native mobile wrapper context.

### 7.3.3. Device Hardware Integrity & UI Boundaries
- **Safe Area Insets Inclusion:** All mobile viewport layouts must inject Tailwind padding utility constraints mapping strictly to CSS environment variables: `pt-[env(safe-area-inset-top)]` and `pb-[env(safe-area-inset-bottom)]`. This prevents UI elements from being clipped by hardware notches, dynamic islands, or software navigation bars.
- **Hardware Back Button Control:** For Android-targeted clients, views must implement localized lifecycle wrappers using Capacitor's `App.addListener('backButton', ...)` interface. Dynamic check-in screens or processing dialog modals must consume the hardware back-press signal to close the modal component natively instead of dropping the entire application state.

---

## 8. CONTEXT ARCHITECTURE & COMPREHENSIVE DTO-MAPPER PATTERNS

### 8.1. Data Translation Boundary Encapsulation
Database schemas and persistent entity structures must never leak into the open public layer. All inbound ingestions and outbound responses are mapped through dedicated, immutable Data Transfer Objects (DTOs).
- Mapping Engine: Utilizes MapStruct configured explicitly with componentModel = "jakarta". This forces compile-time mapping code generation to avoid reflection overhead and optimize data transfer speed.
- Validation Constraint Layer: Implements jakarta.validation.constraints properties (@NotNull, @NotBlank, @Size, @Pattern) across all boundary DTO mappings, evaluated natively at controller thresholds via the @Valid interceptor.

### 8.2. Normalized Packaging Architecture Topology
All system components (Services, Repositories, Controllers, Entities, DTOs, Mappers) must reside precisely within the designated folder layouts underneath the mandatory enterprise root namespace:
```
org.nlh4j.saas.membership.hub
├── config          # Enterprise Global Infrastructure Configurations (Security, Kafka, Multi-Tenancy Engine)
├── domain          # Persistent Core Entities and System State Enums
├── repository      # Data Access Layer leveraging panache abstractions (PanacheRepositoryBase)
├── service         # Structural Business Domain Contracts and Asynchronous Core Implementation
├── web             # Inbound API Resource Mapping and Endpoint Ingestion Controllers
│   ├── dto         # Immutable Request and Response Models
│   └── mapper      # Compile-Time Structural MapStruct Mappers
└── integration     # Third-Party External System Bridges (ZaloClient, FCMClient, FirebaseVerifier)
```

## 8.3. AUTOMATED API SPECIFICATION VIA OPENAPI & SWAGGER UI ENFORCEMENT

To ensure seamless frontend client integration and clarify multi-system contracts, all generated backend API endpoints must enforce automated OpenAPI 3.0 documentation powered by the `quarkus-smallrye-openapi` ecosystem extensions.

### 8.3.1. Mandatory Annotation Rail Rules at Web Resource Layers
When provisioning codebases for Controller/Resource boundary classes, the Coder Agent must decorate endpoint blocks with explicit microprofile OpenAPI annotations to guarantee structural validation visibility inside Swagger UI:
- **Operation Metadata (`@Operation`):** Every endpoint mapping requires mandatory `summary` (concise functional brief) and `description` (detailed technical execution lifecycle overview) parameters defined in English.
- **Response Matrix Mapping (`@APIResponse`):** Endpoints must declare a minimum of two explicit response schemas: `@APIResponse(responseCode = "200", description = "Success")` bound to successful mutations, alongside distinct failure profiles (`400` for validation rejections, `401`/`403` for security/tenant anomalies, `500` for fatal engine rejections) mapping structural error DTO contents.
- **Parameter Validation Metadata (`@Parameter`):** Inbound variables captured via `@PathParam`, `@QueryParam`, or transport headers must embed an explicit `@Parameter(description = "...", required = true)` controller node to enforce automated typing boundaries within the interactive visual playground dashboard.

### 8.3.2. Visual Environment Interception & Production Sandboxing
- **Multi-Tenant Global Scheme Injection:** The generated Swagger UI configuration engine must register a global security scheme component for both `X-Tenant-Id` and `Authorization` Bearer tokens. This enables remote testing engineers to pass dynamic authentication states directly across browser testing loops.
- **Production Endpoint Cloaking:** Application property variables must configure the OpenAPI compilation outputs to actively serve endpoints inside local Dev/Staging environments but completely disable runtime Swagger access paths upon production GKE cluster migrations to mitigate architecture structure leakage vulnerabilities.

---

## 9. ENTERPRISE SECURITY CODES & INJECTION COUNTERMEASURES

### 9.1. SQL Injection (SQLi) Absolute Countermeasures
- **Parameterized Boundaries:** Raw SQL string concatenation, custom dynamic string formatting (`String.format()`), or string interpolations inside Panache/Hibernate queries are strictly prohibited. 
- **Reactive Parameter Bindings:** All query operations must explicitly bind parameters using named attributes or ordered indices through Panache positional parameters (e.g., `repository.find("tenantId = ?1 AND email = ?2", tenantId, email)`).
- **Dynamic Sort Injection Protection:** Dynamic sorting inputs from user requests must be strictly validated against an explicit whitelist of entity field names. Direct injection of untrusted text strings into `Panache.sort()` or `ORDER BY` predicates is forbidden.

### 9.2. Cross-Site Scripting (XSS) & Content Security Policy (CSP)
- **Immutable Context Sanitization:** Any client-side data binding inside the Next.js runtime must rely on native React JSX token auto-escaping mechanisms. 
- **Dangerous Render Restrictions:** The utilization of Next.js `dangerouslySetInnerHTML` is completely banned across all application interfaces, unless the input is proven to pass through a backend-verified, cryptographically signed HTML sanitizer whitelist wrapper.
- **Strict Content Security Headers:** The Edge Middleware framework must inject non-negotiable HTTP security headers into every routing response. This includes: `Content-Security-Policy` (strictly enforcing script-src restrictions, forbidding `unsafe-inline` or unsafe source execution paths), `X-Frame-Options: DENY` (anti-clickjacking), and `X-Content-Type-Options: nosniff`.

### 9.3. Multi-Tenant Cross-Origin Resource Sharing (CORS) Security Rails
- **Wildcard Prohibition:** Universal wildcard declarations (`Access-Control-Allow-Origin: *`) are completely restricted inside production runtime environmental blocks.
- **Dynamic Tenant Origin Validation:** CORS interceptor filter components inside Quarkus must read authorized domain metrics strictly linked to individual registered profiles inside the `sys_tenants` active configurations pool. 
- **Credential & Preflight Safety:** Responses validating tenant cross-origins must preserve strict security boundaries, allowing `Access-Control-Allow-Credentials: true` only when the specific inbound origin perfectly intersects with the verified workspace domain registry.

### 9.4. Zero-Leak Log Scrubbing & PII Data Masking Engines
- **Non-Negotiable Log Sanitization:** Personal Identifiable Information (PII) including plain-text passwords, Firebase/Google/Facebook authorization tokens, JWT secret payload keys, credit card digits, and Zalo API connection hashes must never leak into the system console or centralized logging pipelines.
- **Automated Masking Interceptors:** All incoming Web Resource DTO controllers and outbound integration integrations (ZaloClient, FCMClient) must utilize structured logging interceptors or customized Jackson annotation serializers (`@JsonSerialize`) to apply automated masking filters (e.g., replacing raw password inputs with `[MASKED_SECURITY_CREDENTIAL]`).
- **Cryptographic Trace Masking:** Exception logs captured by the `Reviewer & Fixer Agent` during internal execution failures must never print raw dynamic runtime variables alongside stack traces to shield operational metrics against downstream security analysis.

### 9.5. Cryptographic Boundary Isolation & Token Verification Rails
- **Cryptographic Asymmetric Controls:** Verification of downstream internal tokens requires strict RS256 algorithm enforcement utilizing distinct public keys matching specific environment blocks. Downstream validation filtering components must actively verify the signature, the `exp` expiration threshold, and match the target embedded `tenant_id` claim.
- **QR Code Replay-Attack Infrastructure:** The validation filter processing inbound QR scan requests must instantly verify the temporal threshold bound to the high-precision timestamp payload. Scans reaching backend servers after more than 30 seconds since initial client cryptographic compilation must trigger immediate validation rejection (`400 Bad Request`) to mitigate screenshot or recording replay threats.

---

## 10. REACTIVE Enterprise BUSINESS CORE CODE SAMPLE

### 10.1. Primary Ingestion Gate Service Contract
Target Path: ./sources/backend/src/main/java/org/nlh4j/saas/membership/hub/service/impl/AttendanceServiceImpl.java
```
package org.nlh4j.saas.membership.hub.service.impl;

import io.quarkus.hibernate.reactive.panache.common.WithTransaction;
import io.smallrye.mutiny.Uni;
import jakarta.enterprise.context.ApplicationScoped;
import jakarta.inject.Inject;
import org.eclipse.microprofile.reactive.messaging.Channel;
import org.eclipse.microprofile.reactive.messaging.Emitter;
import org.nlh4j.saas.membership.hub.domain.AttendanceLog;
import org.nlh4j.saas.membership.hub.repository.AttendanceLogRepository;
import org.nlh4j.saas.membership.hub.service.AttendanceService;
import org.nlh4j.saas.membership.hub.web.dto.CheckinRequest;
import org.nlh4j.saas.membership.hub.web.dto.CheckinResponse;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.util.UUID;

/**
 * Core Implementation of Attendance Validation Processing Service.
 * Implements strict non-blocking reactive paradigms via SmallRye Mutiny.
 *
 * @author Principal Spring Boot & Quarkus Engineer
 */
@ApplicationScoped
public class AttendanceServiceImpl implements AttendanceService {

    @Inject
    AttendanceLogRepository attendanceLogRepository;

    @Inject
    @Channel("attendance-events")
    Emitter<String> kafkaEmitter;

    @Override
    @WithTransaction
    public Uni<CheckinResponse> processStudentCheckin(CheckinRequest request) {
        LocalDate today = LocalDate.now();

        return attendanceLogRepository.findDuplicateCheckin(request.getTenantId(), request.getStudentId(), today)
                .flatMap(isDuplicate -> {
                    if (isDuplicate) {
                        CheckinResponse redundantResponse = new CheckinResponse();
                        redundantResponse.setStatus("SUCCESS");
                        redundantResponse.setMessage("Attendance already recorded for today.");
                        redundantResponse.setIsDuplicatedScan(true);
                        return Uni.createFrom().item(redundantResponse);
                    }

                    AttendanceLog newLog = new AttendanceLog();
                    newLog.setId(UUID.randomUUID().toString());
                    newLog.setTenantId(request.getTenantId());
                    newLog.setStudentId(request.getStudentId());
                    newLog.setCardId(request.getCardId());
                    newLog.setCheckinDate(today);
                    newLog.setCheckedInAt(LocalDateTime.now());

                    return attendanceLogRepository.persist(newLog)
                            .map(persistedLog -> {
                                String jsonPayload = String.format(
                                    "{\"eventId\":\"%s\",\"tenantId\":\"%s\",\"studentId\":\"%s\"}",
                                    persistedLog.getId(), persistedLog.getTenantId(), persistedLog.getStudentId()
                                );
                                kafkaEmitter.send(jsonPayload);

                                CheckinResponse successResponse = new CheckinResponse();
                                successResponse.setStatus("SUCCESS");
                                successResponse.setMessage("Check-in processed successfully.");
                                successResponse.setIsDuplicatedScan(false);
                                return successResponse;
                            });
                });
    }
}
```

## 10.2. Administrative Membership Core Engine & Zalo Activation Service
Target Path: ./sources/backend/src/main/java/org/nlh4j/saas/membership/hub/service/impl/AdminMembershipServiceImpl.java
```
package org.nlh4j.saas.membership.hub.service.impl;

import io.quarkus.hibernate.reactive.panache.common.WithTransaction;
import io.smallrye.mutiny.Uni;
import jakarta.enterprise.context.ApplicationScoped;
import jakarta.inject.Inject;
import org.eclipse.microprofile.reactive.messaging.Channel;
import org.eclipse.microprofile.reactive.messaging.Emitter;
import org.nlh4j.saas.membership.hub.repository.StudentCardRepository;
import org.nlh4j.saas.membership.hub.service.AdminMembershipService;
import org.nlh4j.saas.membership.hub.web.dto.ZaloNotificationRequest;

/**
 * Enterprise Service executing Administrative Membership operations.
 * Handles strict multi-tenant analytics and proactive Zalo OA event triggering.
 *
 * @author Principal Spring Boot & Quarkus Engineer
 */
@ApplicationScoped
public class AdminMembershipServiceImpl implements AdminMembershipService {

    @Inject
    StudentCardRepository studentCardRepository;

    @Inject
    @Channel("notification-zalo-personal")
    Emitter<String> zaloPersonalEmitter;

    @Override
    @WithTransaction
    public Uni<Void> triggerManualExpirationNotice(ZaloNotificationRequest request) {
        String jsonPayload = String.format(
            "{\"tenantId\":\"%s\",\"studentId\":\"%s\",\"templateType\":\"EXPIRATION_ALERT\",\"dueDate\":\"%s\"}",
            request.getTenantId(), request.getStudentId(), request.getDueDate()
        );
        
        zaloPersonalEmitter.send(jsonPayload);
        return Uni.createFrom().voidItem();
    }
}
```

---

## 11. AUTOMATED CODE GENERATION RAIL GUARDS (SAFETY RAILS)

The generative AI model must pass all functional requests through the following criteria:
1. No Explanations Policy: Do not wrap code responses with verbose text summaries or introductory outlines. Output pure source blocks containing only file indicators and pristine technical logic.
2. Context Retention Integrity: Read, parse, and incorporate contextual states generated in previous micro-steps (located under .ai-agents/contexts/) before attempting file generation to ensure structural and datatype alignment.
3. Mandatory English Javadocs: Every class structural layout, entity configuration, method contract, and algorithmic fork must embed formal inline descriptors or Javadocs written exclusively in English.
4. Single-file Code Block Isolation: Wrap every code block layout mapping to physical files into unified markdown wrappers tagged with its exact physical destination directory on line 1.

## 12. TESTING STRATEGY & HIGH-FIDELITY TESTING BLUEPRINTS
- **Code Coverage Mandate:** Every business service and custom boundary extension must maintain an automated code coverage metric strictly above 85%.
- **Backend Component Integration Tests:** Leverage Quarkus Dev Services combined with active Testcontainers instances to spin up real transient PostgreSQL and Apache Kafka instances. Testing via memory database simulations (such as H2) is strictly forbidden to preserve reactive transactional authenticity.
- **Frontend Interception & Unit Scenarios:** Employ Jest and React Testing Library to intercept localized router states and validate hydration states. E2E verification paths must compile through headless Playwright environments mimicking cellular shell devices.

## 13. MOBILE HYBRID ARCHITECTURE & NATIVE DEVICE BRIDGING
- **Viewport Layout Adaptation:** Mobile interfaces running within the Capacitor shell layer must strictly utilize CSS environment padding constants (`pt-[env(safe-area-inset-top)]`, `pb-[env(safe-area-inset-bottom)]`) to prevent clipping under phone cameras or system home bars.
- **Hardware Event Trapping:** Android back button parameters must execute via registered listeners bound to Capacitor `App.addListener('backButton', ...)`. System hardware back clicks must interact locally to hide floating overlay widgets or modal fragments without causing application crashes.

## 14. CUSTOM CRM GATEWAY & CROSS-CHANNEL MESSAGING BRIDGE
- **Multi-Channel Dispatch Execution:** The message pipeline must execute non-blocking asynchronous streaming dispatches across three target configurations concurrently: individual student logs via Zalo OA, class metrics streams routed directly into Zalo Group CRM, and real-time remote system alerts pushed via Firebase Cloud Messaging (FCM).
- **Poison Pill Containment & Failure Isolation:** All consumers listening over integration brokers must deploy dedicated Error Retries (exponential backoff profile capped at 3 validation attempts) isolated from primary execution nodes by structural Dead Letter Queues (DLQ).

## 15. ENTERPRISE PACKAGE TOPOLOGY & CONTEXT DATA-MAPPING RULES
- **Package Hierarchy Constraints:** Every repository, entity model, processing service, and integration client must reside strictly inside the explicit folder structures underneath the enterprise root namespace: `org.nlh4j.saas.membership.hub.*`.
- **Data Transfer Object (DTO) Boundary Encapsulation:** Live database entities must never leak outside transactional boundary definitions. All inbound payloads and outbound responses must pass through immutable DTO records mapped exclusively via compile-time MapStruct components.

## 16. STRICT MULTI-AGENT OPERATION RULES (PIPELINE EXECUTION)
Every development issue dispatched to the system must execute through an isolated sequential 4-Agent pipeline. No execution step can be skipped:
- **Coder Agent**: Reads the main `context.en.md` and day context slices. Acts strictly as a Principal Quarkus & Cloud Native Architect (completely avoiding Spring Boot idioms). Generates 100% production-ready, non-blocking domain code using Mutiny and Jakarta EE standards, and outputs it into the exact path specified by the `target_component` key.
- **Tester Agent:** Ingests the generated domain file and writes complete, high-fidelity automated test suites to the exact path defined by `test_component`. No fake assertions or empty blocks allowed.
- **Reviewer & Fixer Agent:** Monitors compilation logs and test run outputs. Executes up to 3 automated repair attempts to secure raw queries, eliminate JWT loopholes, and clean compiler rejections.
- **Documentation Agent:** Reviews the approved codebase and tests to automatically compile clean, granular markdown technical specs into the exact path specified by `doc_component`.
- **Deploy Agent (GCP):**
  * Role: Cloud Infrastructure Registry Engineer.
  * Task: Activated exclusively via the manual execution toggle flag `--release`. Authenticates dynamically against Google Cloud Platform backend instances, compiles optimized application layer components into GraalVM native Docker containers, and pushes images directly to Google Artifact Registry (GAR).
- **Deploy Agent (GKE):**
  * Role: Kubernetes Container Release Engineer.
  * Task: Activated exclusively via the manual execution toggle flag `--release`. Connects securely to the live GKE cluster endpoint, applies raw cloud infrastructure manifests (`infrastructure/k8s`) directly, or triggers safe zero-downtime rolling-update rollouts commands on live compute pod pools.
- **DockerHub Agent:**
  * Role: Cross-Platform Public/Private Cloud Registry Engineer.
  * Task: Trigger manually via the manual execution toggle flag `--release`. Responsible for processing dynamic credentials fetched straight from `DOCKERHUB_SECRETS`, executing non-interactive authorization handshakes, compiling dual multi-stage native images containers for both Backend (GraalVM Mandrel) and Frontend (Next.js production runtime bundle assets), and pushing artifacts cleanly to Docker Hub registries under the specified target namespace.

## 17. AUTOMATED DAILY SESSION GIT BRANCH MANAGEMENT COMPLIANCE FLOW

To drastically optimize token consumption for free-tier AI model accounts, mitigate structural Context Bloat, and ensure complete isolation of stable code states, the orchestrator system must strictly enforce the following automated Git branching compliance flow after every operational day:

### 17.1. Branch Forking and Isolation Controls
- **Immutable Source Base:** All micro-step execution processes for any specific day (Day X) are strictly forbidden from committing directly into main repository targets. Every operation must initialize by programmatically forking a dedicated, independent feature branch directly from the root base: `features/development`.
- **Daily Naming Convention:** The newly provisioned target daily workspace branch must strictly comply with the following explicit alphanumeric formatting blueprint: `features/development-day-X` (e.g., `features/development-day-1`, `features/development-day-2`).

### 17.2. Strict Upstream Validation Guard Pipeline Gates
The Orchestrator runner engine is absolutely restricted from invoking any git commit or push sequences to remote repositories until the day's artifacts successfully clear the following multi-agent automated validation gates:
1. **Compilation Validation Gate:** The Java Backend structure must pass the native compilation loop execution command `mvn clean test-compile` with an absolute zero termination error state (Exit Code 0). The Next.js Frontend module must fully execute `npm run build` clearing all critical syntax rejections.
2. **Automated Testing Validation Gate:** The Tester Agent must generate high-fidelity, real operational test framework structures achieving an absolute automated code coverage metric of >= 85% with zero test case failures.
3. **Automated Fixer Validation Gate:** The Bug Fixer Agent must iteratively resolve and clean all syntax anomalies, compiler diagnostics logs, and unhandled logic exceptions until the compiler returns a completely clean build state.

### 17.3. Automated Push Routine & Token Memory Flushing Execution
- **Automated Single Commit Push:** Immediately upon verification that all pipeline validation gates defined in Section 16.2 report a pristine clean state, the engine must bundle all core implementation components (`target_component`), testing suites (`test_component`), and technical documentations (`doc_component`) into a single, atomic commit message block: `feat(day-X): complete enterprise sub-tasks for day X [AI Pipeline]`, and securely push the `features/development-day-X` branch to the remote origin server.
- **Context Reseeding & Memory Flushing:** The ongoing chat session context for Day X is officially terminated and cleared here. The raw implementation files generated are compacted down into an ultra-lightweight text summary outline mapping out exposed APIs, models, and contracts, saved directly under the system path directory: `.ai/.plan/.context/history_states/dayX_state.md`. For the subsequent sequence initialization phase (Day X+1), the incoming AI Agent must only ingest this lightweight history summary block alongside the immutable blueprints to cut token expenditure metrics by over 80%.




